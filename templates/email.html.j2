<!DOCTYPE html>
<html>
<head>
<style>
  body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; line-height: 1.6; color: #333; }
  .container { max-width: 600px; margin: 0 auto; padding: 20px; border: 1px solid #eee; border-radius: 8px; }
  .header { background: #00d09c; color: white; padding: 20px; border-radius: 8px 8px 0 0; }
  .theme-card { border-left: 4px solid #00d09c; background: #f9f9f9; padding: 15px; margin: 15px 0; }
  .footer { font-size: 0.8em; color: #777; margin-top: 30px; border-top: 1px solid #eee; padding-top: 10px; }
  .btn { display: inline-block; padding: 10px 20px; background: #00d09c; color: white; text-decoration: none; border-radius: 4px; font-weight: bold; margin-right: 10px; }
  .btn-outline { display: inline-block; padding: 10px 20px; border: 1px solid #00d09c; color: #00d09c; text-decoration: none; border-radius: 4px; font-weight: bold; }
</style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>{{ product_name }} Weekly Pulse</h1>
      <p>{{ iso_week }}</p>
    </div>
    
    <p>Hi Team,</p>
    <p>Here are the top themes discovered from this week's reviews:</p>
    
    {% for theme in themes %}
    <div class="theme-card">
      <strong>{{ theme.label }}</strong> ({{ theme.review_count }} reviews)
      <p>{{ theme.description }}</p>
    </div>
    {% endfor %}
    
    <p>
      {% if dashboard_link %}
      <a href="{{ dashboard_link }}" class="btn">View in Emerald Ledger</a>
      <a href="{{ doc_link }}" class="btn-outline">Open Google Doc</a>
      {% else %}
      <a href="{{ doc_link }}" class="btn">Read Full Report on Google Docs</a>
      {% endif %}
    </p>
    
    <div class="footer">
      <p>This is an automated report generated by Antigravity Pulse Agent.</p>
      <p>Product: {{ product_name }} | ISO Week: {{ iso_week }}</p>
    </div>
  </div>
</body>
</html>
